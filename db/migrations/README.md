# ScholarSphere Migrations

This directory contains an ordered set of `*.sql` scripts to create and modify the architecture of ScholarSphere's DB.

## Naming Conventions

Each script follows the convention...

`{step}_{purpose}.sql`